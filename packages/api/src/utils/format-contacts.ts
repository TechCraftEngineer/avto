/**
 * Re-export from shared for backwards compatibility.
 * Используйте @qbs-autonaim/shared для новых импортов.
 */
export {
  type Contact,
  type ContactsData,
  formatContacts,
  getPrimaryContacts,
  mergeContacts,
  normalizeToContactsData,
} from "@qbs-autonaim/shared";
