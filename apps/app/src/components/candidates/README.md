# Candidates Domain

Компоненты и логика для работы с кандидатами и их данными.

## Структура

```
candidates/
├── components/          # React компоненты (21 компонентов)
├── hooks/               # Кастомные React хуки
├── utils/               # Утилитарные функции
├── types/               # TypeScript типы
└── index.ts             # Экспорты домена
```

## Компоненты

Домен содержит 21 компонентов для работы с кандидатами и их данными.

## Использование

```typescript
import { CandidateCard } from '@/components/candidates';
```

## Архитектура

Домен следует принципам:
- **Доменная архитектура** - компоненты сгруппированы по бизнес-логике
- **Прямые импорты** - оптимизация bundle size
- **Server Components** - по умолчанию для производительности
- **TypeScript** - строгая типизация
