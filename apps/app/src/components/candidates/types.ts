export type {
  FunnelActivity,
  FunnelCandidate,
  FunnelCandidateDetail,
  FunnelComment,
  FunnelStage,
} from "./components/types/types";

export { STAGE_COLORS, STAGE_LABELS } from "./components/types/types";
